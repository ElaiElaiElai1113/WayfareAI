{
  "name": "wayfare-ai-worker",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "wrangler dev --config ../wrangler.toml",
    "build": "wrangler deploy --dry-run --config ../wrangler.toml"
  },
  "dependencies": {
    "hono": "^4.7.2",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250224.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.110.0"
  }
}

